​Para que serve #include <stdio.h>​Onde o programa começa ?

ela é feita pra que o programa consiga interagir com o usuario
e ela começa a ser executado no main.

O que acontece se remover o return 0?

não acontece nada de mais e o programa roda normalmente

​O que acontece se remover o \n

faz com que o conteúdo subsequente seja exibido na mesma linha,
em vez de pular para a próxima.
